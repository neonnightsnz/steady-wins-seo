# Domain Docs

Before exploring, read `CONTEXT.md` and relevant ADRs under `docs/adr/` when they exist.

This repository uses the single-context layout:

- `/CONTEXT.md`
- `/docs/adr/`

Use the glossary vocabulary from `CONTEXT.md` when it exists, and flag conflicts with existing ADRs.
