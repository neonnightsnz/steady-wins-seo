export interface Location {
  name: string;
  slug: string | null;
  note: string;
}

export const locations: Location[] = [
  {
    name: '[Service area]',
    slug: null,
    note: '[Confirm whether enough unique local evidence exists for a page]'
  }
];
